import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.StudyHub.app',
  appName: 'StudyHub',
  webDir: 'dist/employee-app/browser', // 👈 this is important
};

export default config;
